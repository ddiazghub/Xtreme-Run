/// <summary>
///     The different game scenes that the main scene can have as children.
/// </summary>
public enum GameScenes {
    PROFILE_SELECT,
    MAIN_MENU,
    LEVEL1,
    LEVEL2,
    LEVEL3,
}